import { MessageObserverDirective } from './message-observer.directive';

describe('MessageObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageObserverDirective();
    expect(directive).toBeTruthy();
  });
});
